import React, {Component} from 'react';
import {StyledToolContainer} from "../../res/styles";

class Base642ImageConverter extends Component {
  render() {
    return (
      <StyledToolContainer>
        Base64 to Image Converter
      </StyledToolContainer>
    )
  }
}

export default Base642ImageConverter;